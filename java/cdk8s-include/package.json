{
  "name": "cdk8s-include-java",
  "version": "0.0.0",
  "main": "index.js",
  "types": "index.d.ts",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "install": "mvn install",
    "import": "cdk8s import && rm -rf imports",
    "build": "npm run import && npm run synth",
    "synth": "cdk8s synth"
  }
}