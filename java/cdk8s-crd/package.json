{
  "name": "@java/cdk8s-crd",
  "version": "0.0.0",
  "main": "index.js",
  "types": "index.d.ts",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "install": "mvn dependency:resolve",
    "import": "cdk8s import && rm -rf imports",
    "build": "mvn clean compile && npm run import && npm run synth",
    "synth": "cdk8s synth"
  }
}