{
  "name": "@java/cdk8s-composition",
  "version": "0.0.0",
  "main": "index.js",
  "types": "index.d.ts",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "install": "mvn dependency:resolve",
    "import": "cdk8s import && rm -rf imports",
    "build": "npm run import && mvn clean compile && npm run synth",
    "synth": "mvn exec:java -Dexec.mainClass=\"com.example.composition.WebServices\""
  }
}