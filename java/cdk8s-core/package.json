{
  "name": "cdk8s-core-java",
  "version": "0.0.0",
  "main": "index.js",
  "types": "index.d.ts",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "install": "mvn install",
    "import": "rm -rf src/main/java/imports && rm -rf src/main/resources/imports  && cdk8s import k8s --language java",
    "build": "npm run import && mvn clean compile && npm run synth",
    "synth": "cdk8s synth"
  }
}