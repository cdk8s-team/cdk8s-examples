{
  "name": "cdk8s-core-java",
  "version": "0.0.0",
  "main": "index.js",
  "types": "index.d.ts",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "install": "mvn install",
    "import": "cdk8s import && rm -rf imports",
    "build": "npm run import && npm run synth",
    "synth": "mvn exec:java -Dexec.mainClass=\"com.example.Main\""
  }
}